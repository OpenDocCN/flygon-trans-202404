- en: Formal SpecsTuesday, January 24th
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 正式规范
- en: Note the formal definitions that were included in the WAE code. They are ways
    of describing pieces of our language that are more formal than plain English,
    but still not as formal (and as verbose) as the actual code.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 注意包含在WAE代码中的正式定义。它们是描述我们语言片段的方式，比普通英语更正式，但仍不像实际代码那样正式（和冗长）。
- en: 'A formal definition of `subst`:'
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: '`subst`的正式定义：'
- en: (`N` is a `<num>`, `E1`, `E2` are `<WAE>`s, `x` is some `<id>`, `y` is a *different*
    `<id>`)
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: （`N`是一个`<num>`，`E1`，`E2`是`<WAE>`，`x`是某个`<id>`，`y`是*不同的*`<id>`）
- en: '[PRE0]'
  id: totrans-4
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: 'And a formal definition of `eval`:'
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: 以及`eval`的正式定义：
- en: '[PRE1]'
  id: totrans-6
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
