- en: Operators - conversion
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 操作符 - 转换
- en: Operators conversion
  id: totrans-1
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 操作符转换
- en: The whole point with this category is to show how easy it is to create Observables
    from something so they can play nice with the operators and well whatever construct
    you come from enables rich composition.
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 这个类别的整个重点是展示如何从某些东西创建 Observables，以便它们可以与操作符很好地配合，以及无论你来自哪种构造都可以实现丰富的组合。
- en: from
  id: totrans-3
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: from
- en: 'In Rxjs4 there existed a bunch of operators with resembling names such as `fromArray()`,
    `from()`, `fromPromise()` etc. All these `from()` operators are now just `from()`.
    Let''s look at some examples:'
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 在 Rxjs4 中存在一堆类似名称的操作符，比如 `fromArray()`、`from()`、`fromPromise()` 等。所有这些 `from()`
    操作符现在都只是 `from()`。让我们看一些例子：
- en: '**old fromArray**'
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: '**旧 fromArray**'
- en: '[PRE0]'
  id: totrans-6
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: '**old fromPromise**'
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: '**旧 fromPromise**'
- en: '[PRE1]'
  id: totrans-8
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: of
  id: totrans-9
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: of
- en: 'The `of` operator takes x number of arguments so you can call it with one arguments
    as well as 10 arguments like so:'
  id: totrans-10
  prefs: []
  type: TYPE_NORMAL
  zh: '`of` 操作符接受 x 个参数，因此你可以用一个参数调用它，也可以用 10 个参数调用，如下所示：'
- en: '[PRE2]'
  id: totrans-11
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: to
  id: totrans-12
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: to
- en: 'There also exist a bunch of operators that allows you go the other way, i.e
    leave the wonderful world of observables and go back to a more primitive state
    like :'
  id: totrans-13
  prefs: []
  type: TYPE_NORMAL
  zh: 还有一堆操作符，允许你走另一条路，即离开观察者的美好世界，回到更原始的状态，比如：
- en: '[PRE3]'
  id: totrans-14
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
