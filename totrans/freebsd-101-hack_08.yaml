- en: Install software
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: Install software
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'If you don''t have any specific requirements for needed software, you can use
    `pkg` utility to install the pre-built package on `FreeBSD`, and it looks like
    using `yum` or `apt-get` on `GNU/Linux` boxes. E.g., install `python` on your
    host:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
- en: 'Now you can run `python` now:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE1]'
  prefs: []
  type: TYPE_PRE
- en: 'But the end logs of installing `python` prompts we still need to install other
    `4` packages: `py-bsddb`, `py-gdbm`, `py-sqlite3` and `py-tkinter`, otherwise
    "`import sqlite3`" will complain:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE2]'
  prefs: []
  type: TYPE_PRE
- en: 'This introduces another installation method: using ports. This method will
    download, compile and install from source code, and the benefit of this approach
    is you can do some customizations if you want. You can find all ports under `/usr/ports`
    directory. Now you begin to install `py-bsddb` (follow this method to set up `py-gdbm`,
    `py-sqlite3` and `py-tkinter`):'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE3]'
  prefs: []
  type: TYPE_PRE
- en: 'After all are installed, we can use `sqlite` in `python` now:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE4]'
  prefs: []
  type: TYPE_PRE
- en: 'The other method is installing from `DVD`:'
  prefs: []
  type: TYPE_NORMAL
- en: (1) Put `DVD` in the drive;
  prefs: []
  type: TYPE_NORMAL
- en: '(2) Mount `DVD`:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE5]'
  prefs: []
  type: TYPE_PRE
- en: '(3) Install the package (E.g., `xorg`):'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE6]'
  prefs: []
  type: TYPE_PRE
- en: 'References:'
  prefs: []
  type: TYPE_NORMAL
- en: '[Packages and Ports: Adding Software in FreeBSD](https://www.freebsd.org/doc/en_US.ISO8859-1/articles/linux-users/software.html);'
  prefs: []
  type: TYPE_NORMAL
- en: '[Get started with FreeBSD: A brief intro for Linux users](http://www.infoworld.com/article/2858288/unix/intro-to-freebsd-for-linux-users.html);'
  prefs: []
  type: TYPE_NORMAL
- en: '[FreeBSD 10 install packages from DVD](https://forums.freebsd.org/threads/44430/#post-300633).'
  prefs: []
  type: TYPE_NORMAL
