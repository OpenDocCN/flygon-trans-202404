- en: Search backwards in csh
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 在csh中向后搜索
- en: Search backwards in csh
  id: totrans-1
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 在csh中向后搜索
- en: 'In `bash`, `Ctrl + R` provides the handy "search backwards" feature which isn''t
    shipped by `csh` by default. If you want to use the similar function in `csh`,
    you can employ `csh` built-in `bindkey` command to implement it:'
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 在`bash`中，`Ctrl + R`提供了方便的“向后搜索”功能，默认情况下`csh`不提供该功能。如果你想在`csh`中使用类似的功能，可以使用`csh`内置的`bindkey`命令来实现：
- en: '[PRE0]'
  id: totrans-3
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: 'Then you can use "search backwards" when pressing `Ctrl + R`:'
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 当按下`Ctrl + R`时，你可以使用“向后搜索”功能：
- en: '[PRE1]'
  id: totrans-5
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: 'References:'
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
  zh: 参考资料：
- en: '[Ctrl-R to search backwards for shell commands in csh](http://stackoverflow.com/questions/1387357/ctrl-r-to-search-backwards-for-shell-commands-in-csh).'
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: '[使用Ctrl-R在csh中向后搜索shell命令](http://stackoverflow.com/questions/1387357/ctrl-r-to-search-backwards-for-shell-commands-in-csh)。'
